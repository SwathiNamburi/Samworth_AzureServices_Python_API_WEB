[EMAIL]
# SMTP server hostname or IP
SMTP_HOST = smtp.office365.com
# SMTP port (25, 587, or 465 for SSL)
SMTP_PORT = 587
# Use STARTTLS? (True/False)
SMTP_USE_TLS = True
# SMTP credentials (if required)
SMTP_USERNAME = swathi.kalidindi@prolifics.com
SMTP_PASSWORD =
# From and To addresses
FROM_EMAIL = swathi.kalidindi@prolifics.com
TO_EMAIL = swathi.kalidindi@prolifics.com

# Optional: override subject/body
SUBJECT = Allure Test Report
BODY = Please find attached the Allure test report generated by the CI pipeline.

# Example usage: python utilities/send_allure_report.py --to customer@example.com

